{
  "name": "multica-unblocker",
  "version": "0.1.0",
  "private": true,
  "description": "Sidecar service for Multica that periodically scans blocked issues, parses mention://issue/<id> blocker links from the description and most-recent 'Blocked:' comment, and auto-resumes the issue when every blocker is done/cancelled.",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
